<section class="section">
  <div class="page-width" style="max-width:820px">
    <div class="section-head">
      <h1>{{ page.title }}</h1>
    </div>
    <div class="rte product__desc" style="color:var(--color-text)">
      {{ page.content }}
    </div>
  </div>
</section>
